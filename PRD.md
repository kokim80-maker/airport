# 인천공항 혼잡도 대시보드 — PRD (Product Requirements Document)

- 문서 버전: v0.1 (Draft)
- 작성일: 2026-07-23
- 작성자: (담당자명 입력)
- 상태: 초안 — 미확정 항목은 "[확인 필요]"로 표시함

---

## 목차

1. 개요
2. 목표 및 성공 지표
3. 대상 사용자 및 사용 시나리오
4. 용어 정의
5. 시스템 아키텍처 개요
6. 데이터 소스 (공공데이터포털 API)
7. 핵심 기능 명세
   - 7.1 오늘/내일 데이터 조회
   - 7.2 날짜·시간 선택 상세 보기
   - 7.3 터미널별(T1/T2) 비교 그래프
   - 7.4 다크모드 전환
   - 7.5 회원가입/로그인 및 즐겨찾기
8. 인증 및 데이터베이스 설계 (Supabase)
9. 환경변수 및 시크릿 관리
10. 화면 정의 (IA / 라우팅)
11. 비기능 요구사항 (성능, 보안, 접근성 등)
12. 에러 처리 및 예외 시나리오
13. 배포 및 운영
14. 범위 제외 (Out of Scope)
15. 마일스톤 (제안)
16. 미해결 질문 (Open Questions) — **코드 작성 전 확인 필요**

---

## 1. 개요

인천국제공항 여객터미널(T1, T2)의 입국장·출국장 혼잡도를 공공데이터포털 API를 통해 조회하고,
사용자가 원하는 날짜와 시간대를 선택해 해당 시점의 혼잡도를 숫자와 그래프로 확인할 수 있는
웹 대시보드 서비스. 로그인한 사용자는 자주 확인하는 시간대를 즐겨찾기로 저장하고 관리할 수 있다.

## 2. 목표 및 성공 지표

| 목표 | 설명 |
|---|---|
| 정보 접근성 | 여객이 공항 방문 전, 터미널·시간대별 혼잡도를 빠르게 파악하도록 지원 |
| 의사결정 지원 | 출국장/입국장 도착 시간을 혼잡도 기반으로 조정할 수 있게 함 |
| 재방문 유도 | 즐겨찾기 기능으로 반복 조회 편의성 제공 |

성공 지표(안, [확인 필요]):
- 오늘/내일 데이터 최초 로딩 성공률
- 즐겨찾기 저장 후 재사용률
- 페이지 로딩 시간 (LCP 등)

## 3. 대상 사용자 및 사용 시나리오

- 인천공항 이용 예정 여객 (출국/입국)
- 공항 관계자, 여행사 등 참고용 조회자 [확인 필요: 주 타겟이 일반 여객인지 B2B 성격도 있는지]

**대표 시나리오**
1. 사용자가 사이트 접속 → 오늘 날짜 기준 T1/T2 입국장·출국장 혼잡도를 바로 확인
2. 특정 날짜(내일)와 시간(예: 14:00)을 선택 → 해당 시점 상세 혼잡도 확인
3. T1과 T2를 그래프로 비교하여 더 여유로운 터미널 파악
4. 다크모드로 전환하여 야간에도 편하게 조회
5. 로그인 후 "내일 오전 9시, T1 출국장"을 즐겨찾기에 저장 → 다음 방문 시 즐겨찾기 목록에서 바로 조회

## 4. 용어 정의

| 용어 | 정의 |
|---|---|
| 혼잡도 | 공공데이터포털 API가 제공하는 터미널별 입국장/출국장 혼잡 수준 (수치 및/또는 등급) |
| 즐겨찾기 | 로그인 사용자가 저장한 (터미널, 구분(입국/출국), 날짜, 시간) 조합. **[변경 이력]** 2026-07-23 중 한때 날짜+시간만 저장하는 것으로 축소됐다가, 같은 날 다시 터미널/구분을 포함하는 원안으로 확정됨 (§8 참고). 이 컬럼 추가 이전에 저장된 기존 행 일부는 터미널/구분이 `NULL`일 수 있음 |
| RLS | Supabase의 Row Level Security. 사용자가 자신의 데이터만 접근 가능하도록 하는 DB 레벨 보안 규칙 |

## 5. 시스템 아키텍처 개요

```
[브라우저]
  ├─ 공공데이터포털 인천공항 혼잡도 API 직접 호출 (Client → 공공API, CORS 허용 시)
  │     ※ API 키는 .env(로컬) / Vercel 환경변수(배포)로 관리, 클라이언트 번들에 노출됨을 전제로 함
  │       → [확인 필요] 노출 허용 여부, 상세는 9장 참고
  │
  ├─ Supabase (Auth + Database)
  │     ├─ Auth: 회원가입/로그인 (이메일/비밀번호 및/또는 소셜 로그인)
  │     └─ Database: favorites 테이블 + RLS 정책 (본인 데이터만 SELECT/DELETE)
  │
  └─ 정적 호스팅: Vercel
```

**아키텍처 상 핵심 제약**
- 백엔드(자체 서버) 없이 프론트엔드 + Supabase(BaaS) 조합의 서버리스 구조.
- 공공데이터 API 호출은 프론트엔드에서 직접 수행 (요청사항에 명시됨) → 이로 인한 리스크는 11장·16장 참고.
- **[확인됨 — 2026-07-23]** 공공데이터포털 API(`apis.data.go.kr/B551177/passgrAnncmt/getPassgrAnncmt`)의 브라우저 직접 호출(CORS)은 **허용됨**. 실제 엔드포인트에 `Origin` 헤더를 포함해 요청한 결과 `Access-Control-Allow-Origin: *`가 응답에 포함되는 것을 확인함 — 즉 브라우저가 이 응답을 차단하지 않는다. 따라서 "프론트엔드가 API를 직접 호출한다"는 아키텍처 전제(§5)는 그대로 유지하며, 프록시(Vercel Serverless Function 등)는 필요 없음.
  - 단, 이 확인은 `apis.data.go.kr` 게이트웨이 공통 CORS 정책에 기반한 것으로, 다른 서비스ID/오퍼레이션을 추가로 연동할 경우에도 동일할 가능성이 높지만 각각 실제 호출로 재확인 권장.
  - 별도 이슈: 현재 발급된 서비스키로 이 API를 호출하면 `401 Unauthorized`가 반환됨(CORS와 무관 — 서버가 아예 응답 바디를 차단하지 않고 401을 정상적으로 반환했으므로 CORS 문제가 아님). 동일한 응답이 의도적으로 틀린 키로 호출했을 때도 나타나 키 형식 문제라기보다는 **data.go.kr 마이페이지에서 이 API(인천공항 여객예고정보)에 대한 활용신청이 아직 승인되지 않았을 가능성**이 큼 — 사용자가 data.go.kr에서 승인 상태를 확인해야 함.

## 6. 데이터 소스 (공공데이터포털 API)

- 제공처: 공공데이터포털 (data.go.kr) — **확정: 서비스ID `B551177`, 오퍼레이션 `passgrAnncmt/getPassgrAnncmt`(인천공항 여객예고정보)**. 엔드포인트: `https://apis.data.go.kr/B551177/passgrAnncmt/getPassgrAnncmt`
- 인증: 서비스 키(`serviceKey` 쿼리 파라미터로 전달). **[확인 필요]** 신규 발급 키는 이 API에 대한 활용신청이 승인되기 전까지 `401 Unauthorized`가 반환됨(승인 후 정상 응답 확인함, 2026-07-23) — 다른 오퍼레이션을 추가할 때도 각각 별도 활용신청/승인이 필요할 수 있음.
- 응답 포맷: **XML 고정.** `_type=json` 쿼리 파라미터를 보내도 무시되고 항상 XML(`Content-Type: application/xml`)로 응답함(실제 호출로 확인, 2026-07-23) — 클라이언트에서 XML 파서(`DOMParser`)로 직접 파싱해야 함.
- 페이징: `numOfRows`/`pageNo` 표준 파라미터 사용. 기본 `numOfRows`가 작아(확인 시점 기본값 10) 하루치 시간대를 다 받으려면 `numOfRows`를 충분히 크게(예: 100) 지정해야 함. `totalCount`로 전체 개수 확인 가능.
- 조회 범위: `selectdate` 파라미터로 0(오늘)/1(내일) 지정 (실제 호출로 확인).
- 응답 항목 (**실제 응답으로 확인함, 2026-07-23** — `response > body > items > item`):
  - `adate`: 조회일자 (YYYYMMDD)
  - `atime`: 시간대, `"00_01"` ~ `"09_10"` 형태의 **1시간 단위** 구간 라벨 (확인된 예시 기준 시간 단위 확정: 1시간)
  - `t1dg1`~`t1dg6`, `t1dgsum1`: T1 터미널 "dg" 그룹 게이트별 값 + 합계
  - `t1eg1`~`t1eg4`, `t1egsum1`: T1 터미널 "eg" 그룹 게이트별 값 + 합계
  - `t2dg1`~`t2dg2`, `t2dgsum2`: T2 터미널 "dg" 그룹 게이트별 값 + 합계
  - `t2eg1`~`t2eg2`, `t2egsum1`: T2 터미널 "eg" 그룹 게이트별 값 + 합계
  - `tmp1`, `tmp2`: 예약 필드로 보임(확인된 응답에서는 항상 빈 값)
  - **[확인 필요]** `dg`/`eg`가 각각 입국장/출국장 중 무엇을 뜻하는지, 값의 단위(여객 수로 추정되나 확정 아님), 게이트별 세부 항목(`dg1~dg6`, `eg1~eg4`)이 실제로 어떤 게이트/구역을 가리키는지는 data.go.kr의 해당 API 상세설명 문서로 재검증 필요. **이 필드가 확정되기 전까지는 `CongestionRecord`(터미널/구역/혼잡도 등급) 타입으로 매핑하지 말 것** — `src/lib/passgrAnncmt.ts`는 원본 필드를 그대로 반환하도록 만들어 둠.
  - 이 응답에는 "여유/보통/혼잡" 같은 등급이나 0-100 정규화 수치가 없다 — §7.2/§7.3의 "혼잡도 값" UI 표현 방식은 위 필드가 확정된 뒤 다시 설계해야 함.
- 갱신 주기: [확인 필요] — 실시간에 가까운지, 특정 배치 주기로 갱신되는지에 따라 화면 안내 문구·새로고침 정책이 달라짐
- CORS: **확인됨(2026-07-23) — 허용**. `Access-Control-Allow-Origin: *` 응답 확인, 프론트엔드 직접 호출 전제 유지 (§5 참고).

## 7. 핵심 기능 명세

### 7.1 오늘/내일 데이터 불러오기
- 최초 진입 시 기본값: 오늘 날짜, 현재 시간(또는 가장 가까운 제공 시간대)의 전체 터미널 혼잡도 표시
- 상단 탭 또는 토글로 "오늘 / 내일" 전환
- 로딩 중 스켈레톤/로딩 인디케이터 표시, 실패 시 에러 메시지(12장 참고)

### 7.2 날짜·시간 선택 후 상세 보기
- 날짜 선택 UI: 오늘/내일 중 선택 (API 제공 범위에 한정)
- 시간 선택 UI: API가 제공하는 시간 단위(예: 1시간 간격)에 맞춘 선택 컨트롤
- 선택 시 해당 시점의 T1/T2 × 입국장/출국장 혼잡도를 숫자(정확한 수치 또는 등급)로 표시

### 7.3 터미널별 비교 그래프
- T1 vs T2를 하나의 그래프에서 비교 (막대 또는 선 그래프 — dataviz 스킬 기준으로 세부 디자인 시 결정)
- 입국장/출국장을 구분해서 표현 (같은 그래프 내 계열 구분, 혹은 탭 전환)
- 선택한 시간대를 그래프 상에서 하이라이트

### 7.4 다크모드 전환
- 라이트/다크 토글 버튼 (헤더 등 고정 위치)
- 시스템 설정(prefers-color-scheme) 감지 후 기본값 적용, 사용자가 수동 전환 시 로컬 저장(localStorage)하여 유지
- 그래프 색상도 다크모드에 맞게 대비 유지 (dataviz 팔레트 가이드 준수)

### 7.5 회원가입/로그인 및 즐겨찾기
- 회원가입/로그인: Supabase Auth 사용, **이메일/비밀번호로 구현됨**(2026-07-23) — 소셜 로그인 추가 여부는 미정(16장 참고)
- 로그인 후 상세 보기 화면에서 터미널·구역별 스탯 타일마다 있는 "즐겨찾기 추가" 버튼으로 (터미널, 구분, 날짜, 시간) 스냅샷을 저장 (§4, §8 참고)
- 즐겨찾기는 **특정 날짜+시간(+터미널+구분) 스냅샷**을 저장하는 개념 (예: "2026-07-24 09:00, T1 출국장"). 요일/시간 패턴 반복 저장은 지원하지 않음.
  - 단, 오늘/내일 데이터만 조회 가능한 서비스 특성상, 즐겨찾기에 저장된 날짜가 지나면 그 항목은 더 이상 유효한 조회 결과를 제공하지 못함 → 목록 화면에서 "지난 항목" 표시 또는 자동 정리 정책이 필요함 (16장 Open Question 참고)
- 즐겨찾기 목록 화면: 본인이 저장한 항목만 조회 가능 (RLS로 서버 단에서 강제)
- 각 즐겨찾기 항목 삭제 가능
- 즐겨찾기 클릭 시 해당 날짜·시간 조건으로 상세 보기 화면 바로 이동 (단, 위에서 언급한 대로 날짜가 지난 경우 데이터 조회 불가 안내)
- 비로그인 사용자는 즐겨찾기 저장/조회만 제한되며, 오늘/내일 조회·상세보기·비교 그래프·다크모드는 로그인 없이 전부 사용 가능 (7.1~7.4 전체 비로그인 허용)

## 8. 인증 및 데이터베이스 설계 (Supabase)

**인증**
- Supabase Auth 사용, 세션 관리는 Supabase 클라이언트 SDK 기본 방식 사용

**데이터베이스 — 실제 생성됨(2026-07-23, Supabase MCP `apply_migration`으로 적용)**

```
table: public.favorites
  id          uuid (PK, default gen_random_uuid())
  user_id     uuid (FK → auth.users.id, not null, on delete cascade)
  adate       date (not null — 특정 날짜 스냅샷을 저장하므로 필수값)
  atime       text (not null)
  terminal    text (nullable, check: 'T1' | 'T2')   -- 2026-07-23 추가
  zone_type   text (nullable, check: '입국장' | '출국장')  -- 2026-07-23 추가
  created_at  timestamptz (not null, default now())
```

**[최종 결정 — 2026-07-23] `terminal`/`zone_type` 컬럼을 추가함.** §4 용어정의 원안대로 "(터미널, 구분, 날짜, 시간)" 조합으로 되돌림 — 처음엔 날짜+시간만 저장하기로 축소했다가(위 이력 참고), 이후 다시 두 컬럼을 추가하기로 결정. **nullable로 추가한 이유**: 컬럼 추가 시점에 이미 실사용자가 저장해둔 즐겨찾기 행이 1건 있었고, 그 행은 어떤 터미널/구역이었는지 알 수 없어(당시 UI가 날짜+시간만 받았음) 임의로 값을 채워 넣지 않고 `terminal`/`zone_type`을 `NULL`로 남겨둠 — 화면에는 "(터미널/구역 미기록)"으로 표시됨. 이후 저장되는 즐겨찾기는 상세 카드의 터미널·구역별 "즐겨찾기 추가" 버튼에서 항상 값을 채워 저장한다.

**RLS 정책 — 실제 적용 및 확인됨(2026-07-23)**
- SELECT: `favorites_select_own` — `auth.uid() = user_id` 인 행만 조회 가능 (역할: `authenticated`)
- INSERT: `favorites_insert_own` — `auth.uid() = user_id` 조건으로 본인 명의로만 생성 가능 (역할: `authenticated`)
- DELETE: `favorites_delete_own` — `auth.uid() = user_id` 인 행만 삭제 가능 (역할: `authenticated`)
- UPDATE: 정책 없음 (즐겨찾기 수정 기능이 없어 의도적으로 비워둠)

## 9. 환경변수 및 시크릿 관리

| 변수 | 용도 | 관리 위치 |
|---|---|---|
| `VITE_PUBLIC_DATA_API_KEY` (또는 프레임워크에 맞는 접두사) | 공공데이터포털 API 인증키 | `.env.local`(로컬), Vercel 프로젝트 환경변수(배포) |
| `VITE_SUPABASE_URL` | Supabase 프로젝트 URL | 동일 |
| `VITE_SUPABASE_ANON_KEY` | Supabase anon(public) key | 동일 |

**주의사항**
- 공공데이터 API 키를 프론트엔드에서 직접 사용하는 구조이므로, 빌드 결과물에 키가 포함되어 브라우저에서 노출됨 (네트워크 탭에서 확인 가능). 이는 요청사항에 따른 설계이나, 트래픽 도용/쿼터 소진 리스크가 있음을 문서에 명시 (11장 참고).
- Supabase anon key는 공개되어도 되는 키이나, RLS 정책이 반드시 활성화되어 있어야 안전함 (RLS 없이는 anon key로 전체 테이블 접근 가능).
- `.env` 파일은 `.gitignore`에 포함하여 저장소에 커밋되지 않도록 함.

## 10. 화면 정의 (IA / 라우팅) — 초안

| 경로 | 화면 | 설명 |
|---|---|---|
| `/` | 메인 대시보드 | 오늘/내일 토글, 전체 터미널 혼잡도 요약, 비교 그래프 |
| `/detail` | 상세 보기 | 날짜·시간 선택 후 상세 수치/그래프, 즐겨찾기 추가 버튼 |
| `/login`, `/signup` | 인증 | Supabase Auth 연동 폼 |
| `/favorites` | 즐겨찾기 목록 | 본인 즐겨찾기 조회/삭제 |

## 11. 비기능 요구사항

- **성능**: [확인 필요] 목표 로딩 시간, 동시 사용자 규모 등 트래픽 가정
- **반응형**: 모바일/데스크톱 대응 여부 [확인 필요]
- **접근성**: 색상 대비(WCAG), 스크린리더 대응 수준 [확인 필요]
- **보안**
  - 공공 API 키 프론트 노출에 따른 쿼터 소진/오남용 가능성 — 필요 시 도메인 리퍼러 제한 등 공공데이터포털 측 옵션 확인 권장
  - Supabase RLS 미적용 시 전체 사용자 즐겨찾기 데이터 노출 위험 → 8장 정책 필수 적용
- **다국어**: 한국어 단일 지원 여부 또는 영어 등 추가 지원 여부 [확인 필요]

## 12. 에러 처리 및 예외 시나리오

- 공공데이터포털 API 장애/타임아웃 시: 에러 메시지 노출 + 재시도 버튼 (자동 재시도 정책은 [확인 필요])
- API 쿼터 초과(429 등) 시 대응 방안 [확인 필요]
- 선택한 날짜/시간에 데이터가 없는 경우(예: 운영 시간 외) 안내 문구
- 비로그인 사용자가 즐겨찾기 버튼 클릭 시 로그인 유도

## 13. 배포 및 운영

- 호스팅: Vercel
- 배포 브랜치 전략: [확인 필요] (예: main 자동 배포 여부)
- 모니터링/로깅: [확인 필요] (Vercel Analytics, Sentry 등 사용 여부)

## 14. 범위 제외 (Out of Scope)

- 혼잡도 임계치 초과 시 푸시/이메일 알림
- 과거 데이터 통계·트렌드 분석 (오늘/내일 외 기간)
- 관리자 페이지(전체 사용자 즐겨찾기 관리 등)
- 다국어 지원 (별도 확정 전까지 기본 제외)

## 15. 마일스톤 (제안)

| 단계 | 내용 |
|---|---|
| M1 | 공공데이터 API 연동 확인 (CORS, 응답 스펙 검증) + 오늘/내일 데이터 표시 |
| M2 | 날짜·시간 선택 상세 보기 + 터미널 비교 그래프 |
| M3 | 다크모드 |
| M4 | Supabase Auth 연동 (회원가입/로그인) |
| M5 | 즐겨찾기 CRUD + RLS 적용 |
| M6 | Vercel 배포 및 환경변수 설정 |

## 16. 미해결 질문 (Open Questions)

### 확정된 항목 (2026-07-23 논의 반영)

- ~~즐겨찾기 저장 개념~~ → **특정 날짜+시간 스냅샷** 저장으로 확정 (7.5, 8장 반영)
- ~~비로그인 사용자의 조회 기능 이용 범위~~ → 오늘/내일 조회·상세보기·비교 그래프·다크모드는 **비로그인도 전부 이용 가능**, 즐겨찾기 저장/조회만 로그인 필요로 확정 (7.5 반영)
- ~~프론트엔드 기술 스택~~ → **React + Vite**로 확정 (아래 스택 표 반영)
- ~~공공데이터포털 API의 브라우저 직접 호출(CORS) 허용 여부~~ → **허용 확인됨** (`Access-Control-Allow-Origin: *`), 프록시 불필요로 확정 (5장, 6장 반영)
- ~~API 활용신청 승인 상태~~ → **승인 완료, 정상 응답 확인함(2026-07-23)** — 처음엔 `401 Unauthorized`였으나 재확인 시 정상 데이터(`resultCode: 00`)로 응답함
- ~~응답 포맷(JSON/XML)과 시간 단위~~ → **XML 고정**(`_type=json` 무시됨), **1시간 단위**(`atime`이 `"00_01"`~`"09_10"` 형태)로 확정 (6장 반영)
- ~~로그인 방식~~ → **이메일/비밀번호로 구현함(2026-07-23)**. Supabase Auth `signUp`/`signInWithPassword`/`signOut` 사용, 회원가입 후 프로젝트 설정상 이메일 확인 없이 즉시 세션 발급됨(실제 테스트 계정으로 확인). 소셜 로그인(Google 등) 추가 여부는 별도 논의 필요(아래).

### 여전히 확인 필요한 항목

1. **[최우선]** 실제 응답 필드 `t1dg1~6`/`t1eg1~4`/`t2dg1~2`/`t2eg1~2`가 각각 입국장/출국장 중 무엇을 뜻하는지, 값의 단위(여객 수로 추정), 게이트별 세부 항목의 의미 — data.go.kr의 API 상세설명 문서로 재검증 필요 (6장 참고). 이 필드가 확정되어야 §7.2/§7.3의 "혼잡도 값"을 실제 데이터로 표시할 수 있음 — 현재 응답에는 "여유/보통/혼잡" 등급이나 0-100 수치가 없으므로, 이 원본 값을 어떤 등급/수치로 변환해 보여줄지도 함께 결정 필요.
2. 데이터 갱신 주기 — 실시간에 가까운지, 배치 주기로 갱신되는지
3. 소셜 로그인(Google 등) 추가 지원 여부 (현재는 이메일/비밀번호만 구현됨)
4. 즐겨찾기에 저장된 날짜가 지난 경우 처리 방식 — 자동 삭제할지, "만료됨" 표시만 하고 사용자가 직접 삭제하게 할지 (7.5 참고)
5. 모바일 반응형 지원 범위
6. 다국어 지원 필요 여부
7. 즐겨찾기 개수 제한 여부
8. 배포 시 브랜치 전략 및 모니터링 도구(Vercel Analytics, Sentry 등) 사용 여부

### 기술 스택 (확정)

| 영역 | 선택 |
|---|---|
| 프론트엔드 | React + Vite |
| 스타일/그래프 | 미정 — 구현 단계에서 결정 (그래프는 dataviz 가이드 기준 적용 예정) |
| 인증/DB | Supabase (Auth + Database + RLS) |
| 호스팅 | Vercel |
